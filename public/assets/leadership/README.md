# Leadership photos

Drop the real photographs here with these exact filenames (JPG, PNG or WebP all work —
just keep the extension matching `photoSrc` in `src/data/leadership.ts`):

- `founder.jpg` — Srafraz Ahmed Bhatti, Founder
- `ceo.jpg` — Ashfaq Ahmad, Chief Executive Officer

No code changes are required. Until a file exists at the expected path, the
`LeadershipPhoto` component automatically shows an elegant placeholder (initials +
"Photo coming soon") instead of a broken image.

If you use a `.png` or `.webp` file instead of `.jpg`, update the matching
`photoSrc` value in `src/data/leadership.ts` to match.
